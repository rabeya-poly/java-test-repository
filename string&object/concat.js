const frist = 'Abid';
const last = 'Nahid';

// const fullnName = frist + last;

const fullName = frist + ' ' + last;
console.log(fullName);
const fullName2 = frist.concat(last)
console.log(fullName2);

// include
console.log(last.includes('x'));

