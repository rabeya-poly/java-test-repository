const computer = {
    brand:  'hp',
    price: 40000,
    processor : 'intel',
    hardDisk : '500',
    monitor: 'sumsung'

}
// console.log(computer);

// in one line output show
const values = Object.values(computer);
console.log(values);
